import { ReadStream, createReadStream, readFileSync } from "fs";

export class TestFiles {
  /*
    pdfName: string;
    pdfPath: string;
    pdfData: string;
    pdfStream: ReadStream;
    pdfLength: number;

    pdfName2: string;
    pdfPath2: string;
    pdfData2: string;
    pdfStream2: ReadStream;
    pdfLength2: number;

    textName: string;
    textPath: string;
    textData: string;
    textStream: ReadStream;
    textLength: number;

    wordName: string;
    wordPath: string;
    wordData: string;
    wordStream: ReadStream;
    wordLength: number;

    excelName: string;
    excelPath: string;
    excelData: string;
    excelStream: ReadStream;
    excelLength: number;

    emlName: string;
    emlPath: string;
    emlData: string;
    emlStream: ReadStream;
    emlLength: number;

    msgName: string;
    msgPath: string;
    msgData: string;
    msgStream: ReadStream;
    msgLength: number;

    jpgName: string;
    jpgPath: string;
    jpgData: string;
    jpgStream: ReadStream;
    jpgLength: number;

    zipName: string;
    zipPath: string;
    zipData: string;
    zipStream: ReadStream;
    zipLength: number;


    constructor() {

        this.pdfName = 'pdf1.pdf';
        this.pdfPath = './' + this.pdfName;
        this.pdfData = readFileSync(this.pdfPath).toString('base64');
        this.pdfLength = this.pdfData.length;

        this.pdfName2 = 'pdf2.pdf';
        this.pdfPath2 = './' + this.pdfName2;
        this.pdfData2 = readFileSync(this.pdfPath2).toString('base64');
        this.pdfLength2 = this.pdfData2.length;

        this.textName = 'txt.txt';
        this.textPath = './' + this.textName;
        this.textData = readFileSync(this.textPath).toString('base64');
        this.textLength = this.textData.length;

        this.wordName = 'wordDoc.docx';
        this.wordPath = './' + this.wordName;
        this.wordData = readFileSync(this.wordPath).toString('base64');
        this.wordLength = this.wordData.length;

        this.excelName = 'excel.xlsx';
        this.excelPath = './' + this.excelName;
        this.excelData = readFileSync(this.excelPath).toString('base64');
        this.excelLength = this.excelData.length;

        this.emlName = 'email.eml';
        this.emlPath = './' + this.emlName;
        this.emlData = readFileSync(this.emlPath).toString('base64');
        this.emlLength = this.emlData.length;

        this.msgName = 'email.msg';
        this.msgPath = './' + this.msgName;
        this.msgData = readFileSync(this.msgPath).toString('base64');
        this.msgLength = this.msgData.length;

        this.jpgName = 'picture.jpg';
        this.jpgPath = './' + this.jpgName;
        this.jpgData = readFileSync(this.jpgPath).toString('base64');
        this.jpgLength = this.jpgData.length;

        this.zipName = 'zip.zip';
        this.zipPath = './' + this.zipName;
        this.zipData = readFileSync(this.zipPath).toString('base64');
        this.zipLength = this.zipData.length;
    };

    getPdfStream() {
        return createReadStream(this.pdfPath);
    }

    getPdfStream2() {
        return createReadStream(this.pdfPath2);
    }

    getTextStream() {
        return createReadStream(this.textPath);
    }

    getWordStream() {
        return createReadStream(this.wordPath);
    }

    getExcelStream() {
        return createReadStream(this.excelPath);
    }

    getEmlStream() {
        return createReadStream(this.emlPath);
    }

    getMsgStream() {
        return createReadStream(this.msgPath);
    }

    getJpgStream() {
        return createReadStream(this.jpgPath);
    }

    getZipStream() {
        return createReadStream(this.zipPath);
    }
*/
}
